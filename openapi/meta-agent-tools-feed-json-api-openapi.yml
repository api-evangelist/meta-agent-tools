openapi: 3.2.0
info:
  title: Meta Agent Tools Feed.json API
  version: 1.0.0
  description: Meta Agent Tools — registry of MCP servers, skills and plugins.
servers:
- url: https://agentalog.com
tags:
- name: Feed.json
paths:
  /feed.json:
    get:
      operationId: get_feed_json
      summary: JSON Feed 1.1 of the most recently published listings — the same stream as the…
      description: 'Returns: `application/feed+json`.'
      security: []
      responses:
        '200':
          description: '`application/feed+json`.'
      tags:
      - Feed.json
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: Guest mr_…, session sess_… or ADMIN_TOKEN.
